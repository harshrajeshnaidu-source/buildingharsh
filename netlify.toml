[build]
  functions = "netlify/functions"
  publish = "public"

[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"

# Route /api/habits → the function
[[redirects]]
  from = "/api/habits"
  to = "/.netlify/functions/habits"
  status = 200
